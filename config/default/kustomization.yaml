apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kjournal-system

namePrefix: kjournal-

labels:
- pairs:
    app.kubernetes.io/instance: logging
    app.kubernetes.io/part-of: kjournal

resources:
- ../apiserver
- ../rbac

components:
- elasticsearch-kjournal
#- gcloud
#- ../components/certmanager
